/**
 * Slack notifier — posts to a channel via incoming webhook.
 *
 * Env: SLACK_WEBHOOK_URL
 * Format: compact blocks with status badge (UNANSWERED), caller, DID, when,
 * last-tried extension, link to `pnpm inspect <call-id>`.
 *
 * STATUS: SKELETON.
 */

export {};

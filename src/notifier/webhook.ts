/**
 * Generic webhook notifier — POSTs JSON to a configurable URL.
 *
 * Use case: wire the on-call rotation's existing tools (PagerDuty,
 * Opsgenie, internal bots) without adding a new first-class channel.
 *
 * STATUS: SKELETON.
 */

export {};

/**
 * Email notifier — SMTP.
 *
 * Env: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, ALERT_EMAIL
 * Format: HTML digest (good for periodic reports, not per-call noise).
 *
 * STATUS: SKELETON.
 */

export {};

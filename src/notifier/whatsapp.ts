/**
 * WhatsApp notifier — replaces the manual WhatsApp report flow.
 *
 * Implementation option: WhatsApp Business Cloud API. Kept behind a
 * feature flag (NOTIFY_WHATSAPP_ENABLED) because it requires a
 * verified business account and template approval.
 *
 * STATUS: SKELETON — implement only if Wellness wants to preserve the
 * current on-call channel instead of migrating to Slack.
 */

export {};

/**
 * OpenAI-compatible provider (kimi / openai / gemini).
 *
 * All three providers speak the OpenAI Chat Completions wire format, so a
 * single implementation using the `openai` SDK covers them; only baseURL
 * and default model differ.
 *
 * STATUS: SKELETON.
 */

export {};

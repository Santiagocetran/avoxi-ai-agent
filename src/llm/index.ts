/**
 * LLM client factory.
 *
 * Same 4-provider matrix as the previous skeleton: kimi / openai / gemini
 * (all OpenAI-wire-compatible) + claude (via a thin adapter).
 *
 * Selected by the LLM_PROVIDER env var. Model and API key come from the
 * corresponding provider-specific envs (see .env.example).
 *
 * STATUS: SKELETON.
 */

export {};

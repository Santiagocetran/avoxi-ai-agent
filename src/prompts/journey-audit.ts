/**
 * Prompt used by cli/inspect.ts to audit a single call in depth.
 *
 * Inputs injected: full AvoxiCdr + reconstructed JourneyTimeline +
 * (optional) recording transcript. Output: narrative audit with a
 * root-cause label drawn from a controlled vocabulary (see
 * domain/missed-call.ts#MissedReason).
 *
 * STATUS: SKELETON.
 */

export {};

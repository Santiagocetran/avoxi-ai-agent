/**
 * Prompt used by cli/watch.ts for a micro-batch of UNANSWERED calls.
 *
 * Narrow instruction: for each call, produce a 2-3 line note for the
 * on-call WhatsApp/Slack replacement — caller number, when it rang,
 * which extensions/queues were tried, where it dropped.
 *
 * STATUS: SKELETON.
 */

export {};

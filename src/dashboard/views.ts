/**
 * Server-side formatting helpers for dashboard API responses.
 *
 * Keeps the HTML/markdown generation out of the route handlers in
 * server.ts so routing stays thin.
 *
 * STATUS: SKELETON.
 */

export {};

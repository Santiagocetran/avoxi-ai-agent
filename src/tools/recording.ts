/**
 * Implements the `get_recording_url` LLM tool.
 *
 * Returns the 24 h pre-signed URL from the CDR. Does NOT download the
 * bytes — download happens only inside the transcription tool, and only
 * when privacy/gate.ts approves.
 *
 * STATUS: SKELETON.
 */

export {};

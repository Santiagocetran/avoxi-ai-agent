/**
 * Implements the `is_on_call` and `classify_call` LLM tools.
 *
 * These are pure / offline — they wrap domain/schedule.ts and
 * domain/missed-call.ts so the LLM can use the same classifications the
 * rest of the system does instead of inventing its own.
 *
 * STATUS: SKELETON.
 */

export {};

/**
 * Implements the `get_call_journey` LLM tool.
 *
 * Input:  { avoxi_call_id }
 * Fetches the single CDR, runs domain/journey.build(), returns the timeline
 * plus the raw CDR for context.
 *
 * STATUS: SKELETON.
 */

export {};

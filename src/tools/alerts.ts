/**
 * Implements `list_alert_logs` LLM tool.
 *
 * Wraps GET /alerts/logs — Avoxi has its own alerting subsystem, and
 * cross-referencing our missed-call detection with their incident log
 * lets the LLM say things like "this gap coincides with alert #abc123
 * (trunk-down)".
 *
 * STATUS: SKELETON.
 */

export {};

/**
 * `pnpm inspect <avoxi_call_id>`
 *
 * Deep audit of a single call — the automation of the manual workflow
 * where a technician looks up a Call ID on the Avoxi panel.
 *
 * Produces:
 *   - Structured call journey timeline (see domain/journey.ts)
 *   - LLM-written narrative: what happened, where it dropped, who was tried
 *   - If answered AND privacy-gate allows it: recording transcription + summary
 *
 * STATUS: SKELETON.
 */

export {};
